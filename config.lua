Config = {}

-- Velocidad máxima (en km/h) a la que el conductor puede apuntar
Config.DriverMaxAimingSpeedKMH = 30.0 -- por ejemplo, 36 km/h (~10 m/s)

-- Velocidad máxima (en km/h) a la que el pasajero puede apuntar
Config.PassengerMaxAimingSpeedKMH = 80.0 -- por ejemplo, 72 km/h (~20 m/s)
